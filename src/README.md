# PayRaise PRO
<h4>📌 The program manages employee salaries, in addition to allowing a percentage increase.<h1>

<h4>📌 Employee data is stored in attributes of the Employee class and instantiated in a list (ArrayList)<h2>

<h4>📌 Each employee has a unique ID<h2>